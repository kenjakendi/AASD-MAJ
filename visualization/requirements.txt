# Dashboard Requirements
# Note: This file is separate from main requirements.txt due to Jinja2 version conflict
# SPADE requires Jinja2==3.0.3, Flask requires Jinja2>=3.1.2
# Dashboard runs in its own Docker container with these dependencies

flask==3.0.0
flask-socketio==5.3.5
flask-cors==4.0.0
python-socketio==5.10.0
eventlet==0.33.3
requests==2.31.0

